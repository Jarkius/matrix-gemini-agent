{
  "name": "matrix-gemini-agent",
  "version": "0.1.0",
  "description": "Morpheus in code - Gemini AI research agent for The Matrix",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir=dist --target=bun"
  },
  "bin": {
    "matrix-gemini": "src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.25.3"
  }
}
